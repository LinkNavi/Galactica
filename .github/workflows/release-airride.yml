name: Release AirRide

on:
  push:
    tags:
      - 'airride-*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release'
        default: '1.0.0'
        required: true

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install deps
        run: sudo apt-get install -y g++ make

      - name: Build AirRide
        run: |
          # Init — use Makefile so all source files are compiled
          cd AirRide/Init
          make
          cd ../..

          # Ctl
          cd AirRide/Ctl && mkdir -p build
          g++ -o build/airridectl src/main.cpp -Wall -Wextra -O2 -std=c++17
          cd ../..

      - name: Package
        run: |
          mkdir release
          cp AirRide/Init/build/airride release/
          cp AirRide/Ctl/build/airridectl release/
          tar -czf airride-${{ inputs.version }}.tar.gz -C release .

      - name: Upload Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: airride-${{ inputs.version }}
          name: AirRide ${{ inputs.version }}
          files: airride-*.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
